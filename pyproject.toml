[project]
name = "research-pipeline"
version = "0.1.0"
description = "Automated research agent that fetches, scores, and summarizes AI & DevSecOps articles into Markdown briefings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0",
    "requests>=2.31",
]
